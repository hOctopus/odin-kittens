<% provide(:title, @kitten) %>

<ul>
  <br><h3><%= "#{@kitten.name}" %></h3>
  <%= "Age: #{@kitten.age} months" %>
  <br><%= "Cuteness factor (1-10 scale): #{@kitten.cuteness}" %>
  <br><%= "Softness factor (1-10 scale): #{@kitten.softness}" %><br><br>
  <ul><%= link_to "edit this sweet woogums", edit_kitten_path(@kitten) %></ul>
  <ul>
    <%= link_to "delete this baby darling (how could you)",
        @kitten, method: :delete, data:
        { confirm: "Sure you want to delete this precious meeper?" } %>
  </ul>
  <ul><%= link_to "safely return to kitteh snuggle home", root_url %></ul>
</ul>
