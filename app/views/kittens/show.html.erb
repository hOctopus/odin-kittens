<% provide(:title, @kitten) %>

<ul><%= "Name: #{@kitten.name}" %></ul>
<ul><%= "Age:  #{@kitten.age}" %></ul>
<ul><%= "Cuteness factor (1-10 scale): #{@kitten.cuteness}" %></ul>
<ul><%= "Softness factor (1-10 scale): #{@kitten.softness}" %></ul>
<ul><%= link_to "edit this sweet woogums", edit_kitten_path(@kitten) %></ul>
<ul>
  <%= link_to "delete", @kitten, method: :delete,
      data: { confirm: "Sure you want to delete this precious meeper?" } %>
</ul>
